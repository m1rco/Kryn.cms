
/*

window.addEvent('domready', function(){
    $$('.publicationNewsListDefaultItem').each(function(item){
        var content = item.getElement('div[class=publicationNewsListDefaultItemRealContent]');
        var _content = item.getElement('div[class=publicationNewsListDefaultItemContent]');
        var link = item.getElement('a[class=publicationNewsListDefaultItemLink]');
        var button = item.getElement('div[class=publicationNewsListDefaultItemMoreBg]');
        if( !button ) return;
        var buttonTop = button.getPosition(_content).y;
        link.addEvent('click', function(){
            if( content.getStyle('height').toInt() != 1 ){
                content.tween('height', 1);
                button.tween('top', buttonTop);
            } else {
                content.tween('height', content.scrollHeight);
                button.tween('top', buttonTop+content.scrollHeight);
            }
        });
        button.getElement('a').addEvent('click', function(){
            link.fireEvent('click');
        });
    });
});

*/